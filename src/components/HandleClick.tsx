'use client';

// the only way I could get this to work was to make it a seperate component with 'use client'
const HandleClick = () => {
    return ( 
        alert('I am an event handler - handleClick.')
    )
    // nodeId = nodeInput
}

export default HandleClick